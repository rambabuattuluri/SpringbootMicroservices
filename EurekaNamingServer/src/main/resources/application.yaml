spring:
  application:
    name: EurekaNamingServer  
server:
  port: 8761    
eureka:
  instance:
    hostname: 192.168.99.100
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/